import type { MockInterviewItem } from '../types.ts'

export const mockInterviewData: MockInterviewItem[] = [
  {
    "id": "mock-1",
    "category": "Technical Round 1",
    "question": "Tell me about a challenging technical problem you solved.",
    "instructions": "Reflect on the question, sketch out your solution, and click Reveal to see the model answer and evaluate your response.",
    "answer": "\"In my last role, we had an ASP.NET Core API serving a report generation feature that was timing out for large datasets. The root cause was a LINQ query loading 100,000 entities into memory before filtering. I solved this in three steps: first, I moved the filtering to the database level by ensuring the LINQ predicates translated to SQL WHERE clauses — used EF Core query logging to verify. Second, I implemented cursor-based pagination so we process 1,000 records at a time. Third, for clients needing the full report, I made the operation async — client submits a job, polls a status endpoint, downloads when ready. Result: response time went from 30s timeout to 200ms for paginated, and background jobs for full reports. This also taught me to always log generated SQL in development.\" Interviewer: \"Explain SOLID with a real example.\" Model Answer: \"Let me use an Order processing system. Before SOLID, we had an OrderService class that validated orders, calculated totals, saved to database, sent confirmation emails, and generated invoices — violating SRP. We split it: OrderValidator, OrderCalculator, OrderRepository, EmailService, InvoiceService. Each has one reason to change. For OCP: we added discount strategies (BulkDiscount, LoyaltyDiscount) without changing OrderCalculator — just implementing IDiscountStrategy. LSP: we had a PremiumOrder extending Order — PremiumOrder.TotalAmount had to behave consistently with Order.TotalAmount, not surprise callers. ISP: IOrderService was split into IOrderQuery and IOrderCommand — read-only services only implement query interface. DIP: OrderService receives IOrderRepository in constructor — testable with InMemoryOrderRepository.\" Interviewer: \"What is async/await and when should you NOT use it?\" Model Answer: \"Async/await enables writing asynchronous code in a synchronous style. The await keyword suspends execution of the current async method until the awaited task completes, without blocking the thread — that thread returns to the thread pool to serve other requests. Don't use async when: operation is CPU-bound and short (overhead of Task infrastructure exceeds benefit). Don't mix with .Result or .Wait() — causes deadlocks in ASP.NET Framework. Don't use async void except for event handlers — exceptions are unobservable. Don't use async in constructors. A common mistake I've seen: async method that starts work then immediately calls .Result on the returned task. This defeats the purpose and can deadlock.\" Interviewer: \"Write a function to find the first duplicate in an array.\" Model Answer: public int? FindFirstDuplicate(int[] nums) { var seen = new HashSet<int>(); foreach (var num in nums) { if (!seen.Add(num)) return num; // HashSet.Add returns false if already present } return null; // No duplicate found } \"Time complexity O(n), space O(n). The HashSet.Add returns false if the element already exists — clean and idiomatic. For a follow-up — if we need O(1) space and numbers are 1 to n, we can use the index negation trick, but that's more complex and modifies the input.\" Interviewer: \"What happens when you type in a URL?\" Model Answer: \"DNS resolution: browser checks local cache, OS cache, then DNS server to get the IP address. TCP handshake: client sends SYN, server ACKs with SYN-ACK, client sends ACK. TLS handshake (for HTTPS): negotiate cipher, exchange certificates, establish symmetric key. HTTP request sent: method, URL, headers including Host, cookies. Server processes: load balancer routes to an application server, ASP.NET Core middleware pipeline runs — routing, auth, controller action, response generation. HTTP response sent with status code, headers, body. Browser renders HTML, downloads CSS/JS, renders DOM.\" Interviewer: \"How would you design a caching strategy for a product catalog?\" Model Answer: \"Product catalog is read-heavy with infrequent updates — ideal for caching. Strategy: Cache-aside pattern with Redis. On read: check Redis first; if miss, query SQL, store in Redis with 15-minute TTL. On update: invalidate specific product's cache key immediately. Category-level cache with 5-minute TTL for listing pages. Use sliding expiration for popular products, absolute for less-accessed. For scale-out: Redis Cluster or Redis Sentinel for high availability. Monitor cache hit ratio — target > 80%. Consider bloom filter to avoid cache-miss floods for non-existent product IDs.\"",
    "suggestedScore": "4/5"
  },
  {
    "id": "mock-2",
    "category": "Technical Round 2",
    "question": "Design a system for firmware update distribution to 1 million IoT devices.",
    "instructions": "Reflect on the question, sketch out your solution, and click Reveal to see the model answer and evaluate your response.",
    "answer": "\"Let me clarify requirements first. Read-heavy (device checking for updates). Write from admin (new firmware release). Reliability critical (partial updates = bricked device). Scale: 1M devices, assume 1% checking per hour = 10K RPS. Architecture: Devices connect to Update Check Service via HTTPS REST. Device sends: deviceId, currentVersion, platform, model. Service responds: hasUpdate, packageUrl (presigned CDN URL), hash, signature. Storage: SQL Server for metadata (packages, device versions, rollout rules). Package files: Azure Blob Storage / S3 CDN-fronted — devices download directly from CDN. Staged rollout: release to 1% of devices first (canary), monitor error rate, auto-rollback if > 5% errors, then 10%, 25%, 100%. Safety mechanisms: - Package signing: private key signs firmware hash; device verifies with embedded public key - Hash verification before install - Rollback: device keeps previous version for 24 hours post-update - Health check after install: if fails within 1 hour, auto-rollback Scalability: Update Check Service scales horizontally (stateless). Redis caches 'latest version per platform' (hot path). CDN absorbs download traffic. Failure modes: What if device updates mid-download? — Resume via HTTP range requests. What if health check fails? — Automatic rollback to previous version. What if 50% of devices fail canary? — Auto-pause rollout, alert engineering team.\" Interviewer: \"You're inheriting a legacy monolith. How do you migrate to microservices?\" Model Answer: \"Strangler Fig pattern — don't rewrite, incrementally extract. Start with understanding the domain: event storming with the team to identify bounded contexts. Phase 1: Build an API Gateway in front of the monolith. All traffic flows through it. Zero risk, buys time. Phase 2: Identify first service to extract. Choose: high-change rate, well-bounded, independent scaling need. Often Notification or Authentication services first — clear boundaries, few dependencies. Phase 3: Extract service — new codebase, own database. Route traffic via gateway. Run both monolith and new service in parallel initially (dark launch). Gradually cut over traffic. Remove dead code from monolith. Phase 4: Repeat, extracting the next bounded context. Key principles: Never share databases between services. Accept eventual consistency. Invest in observability early — distributed tracing (OpenTelemetry) is essential. Test contracts between services (consumer-driven contract tests). Risks to manage: Distributed transaction complexity — plan for sagas early. Operational overhead — invest in Kubernetes/Docker from the start. Don't extract everything at once — team ends up maintaining two complex systems.\" Interviewer: \"How would you ensure data consistency in a microservices system?\" Model Answer: \"ACID transactions are limited to a single database. For cross-service consistency, I use: 1. Saga pattern: choreography for simple flows (service publishes event, another reacts), orchestration for complex flows (saga orchestrator directs each step). 2. Outbox pattern: critical for reliability. When Order Service creates an order, it also writes OrderCreated event to an Outbox table in the same SQL transaction. A background process polls the Outbox and publishes to the message bus. Atomically ensures: order saved AND event published. 3. Idempotent consumers: every consumer handles duplicate messages gracefully — store processed event IDs, skip if seen before. 4. Eventual consistency: design the domain to tolerate it. User sees 'Order Pending' immediately; 'Payment Confirmed' comes seconds later. The UI handles this gracefully. 5. Compensating transactions for rollback: Order placed → Inventory reserved → Payment failed → Compensate: Release inventory, Cancel order. Each step has a compensating action. For strong consistency requirements: co-locate data that must be consistent in the same service/database. Sometimes the right answer is to NOT split across services.\"",
    "suggestedScore": "4/5"
  },
  {
    "id": "mock-3",
    "category": "System Design",
    "question": "Tell me about a time you had a disagreement with a colleague about a technical approach.",
    "instructions": "Reflect on the question, sketch out your solution, and click Reveal to see the model answer and evaluate your response.",
    "answer": "\"On our last project, a senior developer proposed using a flat table structure for a complex hierarchical data model to keep queries simple. I felt this would cause significant problems at scale — specifically data duplication and update anomalies. Rather than escalate immediately, I requested a one-on-one conversation. I came prepared with: a prototype query showing the complexity of the flat approach at 1M rows, a normalized schema alternative with benchmark results, and an acknowledgment of the valid points in the flat approach (simpler initial implementation). We agreed to prototype both approaches with production-like data volumes. After testing, the results showed the normalized approach was 3x faster for writes and maintained data integrity that the flat approach couldn't guarantee. The colleague acknowledged this was the right call. Key lesson: technical disagreements need data, not opinions. And private, direct conversations are better than public debates.\" Interviewer: \"How do you handle working on something completely new to you — like device drivers?\" Model Answer: \"I'd start with structured learning: get the WDK documentation, find the official Microsoft driver samples, and start with the simplest possible thing — the echo sample that loopbacks data from user mode to kernel. That gives me a working foundation to build on. Simultaneously, I'd identify someone on the team with driver experience and ask for a mentor relationship — not to ask basic questions that docs answer, but for architectural guidance and code review. I'd also be upfront with the team about my timeline. For the first 30 days, I'd deliver small, reviewable contributions while learning. I wouldn't take on a critical driver component solo in month one. For this specific role, I've already started on this path — I've been reading the WDK docs and have built a simple character device in Linux. I understand the concepts; now I need production experience.\" Interviewer: \"Where do you see yourself in 3 years?\" Model Answer: \"In 3 years, I see myself as a go-to expert for the systems/driver area on the team — the person others ask about kernel development and low-level architecture. I'd have shipped multiple drivers and have real production experience with both Windows and Linux kernel code. I also want to grow in system design — architecting the monitoring and update distribution platforms that manage our driver ecosystem at scale. My C# and .NET background gives me a strong foundation in architecture and software design. I'm excited to apply those skills to a deeper layer of the stack. This role is exactly the right move to make that happen.\" Interviewer: \"How do you keep your technical skills current?\" Model Answer: \"A few approaches that work for me: I read .NET blog and GitHub discussions for C# — seeing actual design decisions teaches more than tutorials. I follow Stephen Cleary for async patterns and Khalid Abuhakmeh for practical ASP.NET Core. For the driver side: I've bookmarked OSR Online (the premier Windows driver resource) and Bootlin's Linux kernel training materials. I plan to follow kernel mailing lists to see real driver code in review. I also believe in learning by doing — I have a Raspberry Pi setup for Linux kernel module experiments. Playing with real hardware is irreplaceable. And I attend virtual conferences — NDC, .NET Conf. Not just for new features, but for how experienced developers approach problems.\" ==================================================================",
    "suggestedScore": "4/5"
  }
]
